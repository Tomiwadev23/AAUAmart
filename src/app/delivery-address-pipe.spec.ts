import { DeliveryAddressPipe } from './delivery-address-pipe';

describe('DeliveryAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
