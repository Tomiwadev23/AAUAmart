import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AdminsignuppagePage } from './adminsignuppage.page';

describe('AdminsignuppagePage', () => {
  let component: AdminsignuppagePage;
  let fixture: ComponentFixture<AdminsignuppagePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AdminsignuppagePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
